#include <stdio.h>

int main(){
    // int num1, num2;
    // float f;
    // char c;
    // scanf("%d %f %c",&num1,&f,&c);
    // printf("%d %0.2f %c",num1,f,c);

    int a,b;
    char p;
    // scanf("%d%c %d%c",&a, &p, &b, &p);
    // scanf("%d%% %d%%",&a,&b);
    scanf("%da %db",&a,&b);
    // printf("%d%c %d%c", a,p,b,p);
    printf("%da %db",a,b);
    return 0;
}